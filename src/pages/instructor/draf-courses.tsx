import { withInstructorLayout } from "@/layout/instructor"
import { NextPage } from "next"

const DrafCourses: NextPage = () => {
  return (
    <div>DrafCourses</div>
  )
}

export default withInstructorLayout(DrafCourses)